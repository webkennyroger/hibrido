<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:label="hibrido"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        <!-- Adicionado para flutter_local_notifications -->
        <meta-data
            android:name="flutterNotificationIcon"
            android:resource="@mipmap/ic_launcher" />
        <!-- Fim da adição para flutter_local_notifications -->
        <meta-data android:name="com.google.android.geo.API_KEY"
                   android:value="AIzaSyCka52TrH3u26pUrTyRwKkogWB-FdWA2bU"/>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <!-- Permite que o app consulte a instalação do Spotify -->
        <package android:name="com.spotify.music" />
        <!-- Permite que o app consulte a instalação do YouTube Music -->
        <package android:name="com.google.android.apps.youtube.music" />
    </queries>
</manifest>